# rscompiledb
the command line tool used to generate compile_commands.json writing by rust.

firstly, cd to the root diretory of project. then,
```shell
rscompiledb c/cpp -a(optional args) xxx yyy zzz
```
